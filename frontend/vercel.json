{
  "installCommand": "npm ci && npm audit --audit-level=high",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "env": {
    "VITE_API_URL": "https://aria-advisor.onrender.com"
  }
}
